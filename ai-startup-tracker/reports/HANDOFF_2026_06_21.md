# Handoff — AI Startup Tracker (Jun 21 2026)

## Research Goal

Academic paper on **AI's impact on global entrepreneurial activity**:
startup formation rates, founder skills/backgrounds, VC behavior, and geographic clusters.
Needs broad global coverage (all tech, not just AI), founding year, deal data, and eventually founder-level attributes.

---

## Infrastructure

| Resource | Detail |
|---|---|
| **Production DB** | Railway PostgreSQL — `postgresql://postgres:DcnTzVMncslQyHdpliCQpWcWugfJdutm@viaduct.proxy.rlwy.net:19473/railway` |
| **Deploy repo** | `jihwan-victor-park/Tobin_Research_Merge` — Railway auto-deploys from `main` branch |
| **Your repo** | `alastairpage/ai-startup-tracker` — branch `all-sources-batch` |
| **Live app** | `tobinresearchmerge-production-dfda.up.railway.app` |
| **Root dir on Railway** | `ai-startup-tracker/` |

**Push to deploy (all three every time):**
```bash
git push origin all-sources-batch
git push collaborator all-sources-batch:alastair-updated-work
git push collaborator all-sources-batch:main   # ← triggers Railway deploy
```

---

## Database State (Jun 21 2026)

### Companies

| Metric | Value |
|---|---|
| Total companies | 978,162 |
| AI companies | 104,730 (10.7%) — `cb_ai_tagged OR ai_score≥0.5 OR ai_mentioned` |
| With funding records | 243,330 |
| Countries | **166** (cleaned Jun 21 — was 258, fixed ISO codes/cities/junk) |
| With `founded_year` | 925,277 (94.6%) |
| With `categories` (17-category taxonomy) | 912,962 (93%) |
| `naics_code` column | **Being added now** (Revelio enrichment in progress — see below) |

### Sources (non-overlapping, exact)

| Source | Count | Notes |
|---|---|---|
| Crunchbase (`verified_cb`) | 650,200 | Used as enrichment layer |
| PitchBook (`verified_pb`) | 281,395 | Used as enrichment layer |
| Our scrapers (`incubator_signals`) | 14,319 | Not in CB/PB — true unique additions |
| GitHub (`emerging_github`, no incubator_signal) | 32,248 | Not in CB/PB |
| **Total** | **978,162** | Sums exactly |

Scrapers also found 7,202 CB companies and 5,643 PB companies (overlap, not added separately).

### Scraper-unique breakdown by source

| Source | Unique (not in CB/PB) |
|---|---|
| Agentic scrape (296 sites) | 10,210 |
| Techstars | 1,705 |
| Y Combinator | 1,266 |
| Harvard iLabs | 648 |
| Antler | 519 |
| Stanford StartX | 501 |
| Entrepreneur First | 345 |
| MIT Engine | 167 |
| Berkeley SkyDeck | 110 |
| Princeton eLab | 94 |
| Rice OwlSpark | 93 |
| Seedcamp | 80 |

### Geography

| | Count | Share |
|---|---|---|
| US | 366,884 | 37.5% |
| International | 574,826 | 58.8% |
| No country | 36,452 | 3.7% |

10K+ scraper-unique and 21K+ GitHub-unique companies have no country — domain TLD inference (`scripts/infer_country_from_tld.py`) could fill many of these.

### Funding

- 268,981 FundingSignal rows (deal_date, round_type, deal_size — all from PitchBook)
- 243,330 companies with at least one funding record

### Sites (296 total)

| Status | Count | Root cause |
|---|---|---|
| Healthy | 145 | — |
| Broken | 95 | Tavily API timeouts + Anthropic 429s — fixable with retry/backoff |
| Degraded | 36 | Same |
| Pending | 16 | Never run |
| Excluded | 4 | — |

By category: 108 VC portfolios, 31 universities, 28 accelerators, 17 gov programs, 6 discovery aggregators.

---

## Revelio Data (LinkedIn workforce data)

### Files on disk (`~/Downloads/`)

| File | Rows | rcid | Use |
|---|---|---|---|
| `revelio_company_mapping-000000/1/2.parquet` | 23.3M total | — | Company registry: domain, year_founded, NAICS, linkedin_url |
| `revelio_individual_positions-000001.parquet` | 10M | ❌ NULL | Job histories — can't link to companies |
| `revelio_individual_positions-000009.parquet` | 10M | ❌ NULL | Job histories — can't link to companies |
| `revelio_individual_positions-000083.parquet` | 10M | ✅ 100% | Job histories linkable to companies via rcid |
| `revelio_individual_positions-000110.parquet` | 10M | ✅ 100% | Job histories linkable to companies via rcid |
| `revelio_individual_user-000026.parquet` | 10M | — | User profiles: name, gender/ethnicity probs, highest_degree, linkedin_url, country |
| `revelio_individual_user_education-000014.parquet` | 10M | — | Education: university, degree, field, world_rank |

**Key insight:** rcid is the join key linking people → companies. Only shards 000083+ have it populated. Shards 000001 and 000009 are useless for company linking.

### In-progress job (started ~12:45pm Jun 21, still running)

Script: `scripts/enrich_from_revelio.py`

What it does:
- Loads all 3 `company_mapping` shards (23.3M rows)
- Matches to our 947K companies by domain → 611,845 matches (68.5%)
- Updates `founded_year` where NULL in our DB: **3,819 rows**
- Adds `naics_code` column and populates it: **291,686 rows**

Check if done:
```bash
ps aux | grep enrich_from_revelio | grep -v grep
```
If no process, check the DB:
```bash
python3 -c "
from sqlalchemy import create_engine, text
engine = create_engine('postgresql://postgres:DcnTzVMncslQyHdpliCQpWcWugfJdutm@viaduct.proxy.rlwy.net:19473/railway')
with engine.connect() as conn:
    n = conn.execute(text('SELECT COUNT(*) FROM companies WHERE naics_code IS NOT NULL')).scalar()
    print(f'naics_code populated: {n:,}')
"
```

### Second in-progress job (positions analysis)

Script: inline analysis checking shards 000083 and 000110 for founder/exec roles at our companies. Produces counts only (no DB writes). Check output at:
```
/private/tmp/claude-502/.../tasks/bf3uy6yty.output
```

---

## What Was Done This Session

1. **Fixed 4 plotly crashes** (`update_layout()` duplicate `yaxis`/`xaxis` kwargs on Research and Funding tabs) — deployed to Railway
2. **Country normalization** — 258 → 166 distinct countries. Fixed ISO codes (PAK→Pakistan), cities (Texas→United States, Bavaria→Germany), typos (Switerland), junk (Earth's Core Sector, zip codes). Script: `scripts/normalize_countries_railway.py`
3. **Revelio enrichment** — `scripts/enrich_from_revelio.py` running against Railway (in progress)
4. **Saved Railway DB URL** to memory for future sessions
5. **Saved push-to-deploy workflow** to memory

---

## What's Next (priority order)

### Immediate (finish what's in flight)
- [ ] Confirm Revelio enrichment completed (`naics_code` column populated, `founded_year` gap filled)
- [ ] Push final commits to main (deploy)
- [ ] Check positions analysis job output — how many founder/exec roles at our companies

### Short-term
- [ ] **Founder skills pipeline** — the positions shards with rcid (000083, 000110) can link people to our companies. Need to: (1) filter for founder/CEO/CTO roles, (2) join to `individual_user_education` for their degree/field, (3) store in a new `founder_profiles` table. This fills the biggest research gap.
- [ ] **Download more rcid-populated positions shards** — shards 000083+ have rcid; ask Revelio for more in that range. Each shard = 10M more linkable job histories.
- [ ] **Country fill for scraper/GitHub-unique companies** — 31K+ missing country. Run `scripts/infer_country_from_tld.py` against Railway.
- [ ] **Fix broken sites** — 95 broken sites are all Tavily timeouts or Anthropic 429s. Add exponential backoff to `backend/agentic/engine.py`. Unlocks more scraping without any new sites.

### Medium-term
- [ ] **GitHub pipeline** — `github_signals` and `github_repo_snapshots` tables are empty (0 rows). `scripts/github_weekly_discover.py` hasn't run on Railway. Set up as a scheduled job.
- [ ] **Founder profiles table** — new DB table storing `user_id`, `company_domain`, `role`, `start/end date`, `degree`, `field`, `university_rank` for founders at our companies.
- [ ] **Founding year data-lag caveat** — 2022–2026 companies are undercounted (CB/PB lag). Need a note/correction factor in any paper.

### Deferred
- [ ] **React frontend** — `ai-startup-tracker/react-frontend/` exists but not deployed; Railway only serves the Streamlit app
- [ ] **Source_matches table** — schema exists but 0 rows; intended to track CB/PB match provenance

---

## Key Scripts

| Script | Purpose |
|---|---|
| `scripts/normalize_countries_railway.py` | Country cleanup — run after any new bulk import |
| `scripts/enrich_from_revelio.py` | Revelio company_mapping → founded_year + naics_code |
| `scripts/run_orchestrator.py --batch` | Run scraper pipeline on all due sites |
| `scripts/import_crunchbase_companies.py` | Re-import CB (needs `--descs ~/Downloads/organization_descriptions.parquet`) |
| `scripts/import_pitchbook_companies.py` | Re-import PB |
| `scripts/github_weekly_discover.py` | GitHub discovery (not yet run on Railway) |
| `scripts/infer_country_from_tld.py` | Fill missing countries from domain TLD |
| `scripts/diff_sync_companies_to_railway.py` | Sync local → Railway |

## Key Watch-outs

- **Don't re-run CB import** without `--descs ~/Downloads/organization_descriptions.parquet` (not the one in the project root — that's stale)
- **Don't filter AI companies** to `ai_score >= 0.3` alone — combine with `cb_ai_tagged OR ai_mentioned`
- **Don't use `incubator_source` enum** for new scraped sites — use `source_domain` column instead
- **Don't run `python -m backend.orchestrator.orchestrator`** — use `scripts/run_orchestrator.py --batch`
- **Revelio positions shards 000001 and 000009** have no rcid — useless for company linking; only 000083+ shards work
